package com.npst.upiserver.dao;

import java.util.Date;

import com.npst.upiserver.npcischema.Ack;
import com.npst.upiserver.npcischema.ReqRegMob;
import com.npst.upiserver.npcischema.RespRegMob;

public interface ReqRespRegMobDao {
	void insertReqResp(ReqRegMob reqRegMob, RespRegMob respRegMob, Ack ack, Date reqDate);
	void updateRespPay(RespRegMob respRegMob);
	void insertReq(ReqRegMob reqRegMob, Ack ack);
}
