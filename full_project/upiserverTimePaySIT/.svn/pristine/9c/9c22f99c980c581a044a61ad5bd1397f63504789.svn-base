package com.npst.upiserver.dao;

import com.npst.upiserver.npcischema.Ack;
import com.npst.upiserver.npcischema.ReqHbt;
import com.npst.upiserver.npcischema.RespHbt;

public interface ReqRespHbtDao {

	void updateResp(RespHbt respHbt);

	void insertReq(ReqHbt reqHbt, Ack ack);
}
